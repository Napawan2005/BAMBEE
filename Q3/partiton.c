#include <stdio.h>

void main()
{
    int i, j, z, row, col;
    scanf("%d %d", &row, &col);

    for (i = 0; i < row; i++)
    {
        printf("*");
        for (j = 0; j <= col; j++)
        {
            for (z = 0; z < col; z++)
            {
            }
            
        }
        printf("\n");
        
    }
}